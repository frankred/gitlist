<div class="navbar navbar-scroll-top">
	<div class="navbar-inner">
		<div class="container">
			<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </a>
			<a class="brand" href="{{ path('homepage') }}">GitList</a>

			{% if (is_granted('ROLE_USER')    or   is_granted('ROLE_ADMIN'))  %}
			<ul class="nav">
				{% if login_type == 'admin_only' %}
				<li>
					<a href="{{ path('admin_logout') }}">Logout</a>
				</li>
				{% endif %}

				{% if login_type == 'all' %}
				<li>
					<a href="{{ path('logout') }}">Logout</a>
				</li>
				{% endif %}
			</ul>
			{% endif %}

			<div class="nav-collapse">
				<ul class="nav pull-right">
					<li>
						<a href="http://gitlist.org/">About</a>
					</li>
					<li>
						<a href="{{ path('homepage') }}refresh">Refresh</a>
					</li>
					<li>
						<a href="https://github.com/klaussilveira/gitlist/issues/new">Report bug</a>
					</li>
					<li>
						<a href="https://github.com/klaussilveira/gitlist/wiki">Help</a>
					</li>
					<li>
						<a href="{{ path ('adminpanel')}}">Admin</a>
					</li>
				</ul>
			</div>
		</div>
	</div>
</div>
